name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Pasang kebergantungan
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Semak sintaks (compileall semua modul)
        env:
          QT_QPA_PLATFORM: offscreen
        # compileall (berbanding py_compile) menerima direktori dan
        # menyemak semua fail .py secara rekursif tanpa mengimportnya.
        run: python -m compileall -q main.py db.py config.py launcher.py VERSI.py api core ui utils scripts tests

      # Catatan: semak.py melancarkan PustakaApp (perlu paparan / klik
      # dialog deklarasi) - tidak sesuai di CI tanpa sesi interaktif,
      # dan boleh "hang". Akan disambung bila semak.py boleh lari headless
      # atau bila ujian pytest (Fasa 2) wujud.
